Required Addons: axios (npm install --save axios)

Required API tokens: Unsplash API Token

Create file "config.js" in /src direcctory and enter:

------------------------------------

const config = {
  key: [your unsplash API key],
};

export default config;

------------------------------------
