import React from "react";

const ImagePopup = ({isOpen, src}) => {
    if(isOpen) {
        return (
            <div>
                xue
            </div>
        )
    } else {
        return null;
    }
}

export default ImagePopup